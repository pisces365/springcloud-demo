server:
  port: 3344

spring:
  application:
    name: springcloud-config-server #应用的名称
  #连接远程仓库
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/yang-jiening/springcloud-demo.git #https
